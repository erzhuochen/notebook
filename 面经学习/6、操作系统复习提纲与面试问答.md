# 6、操作系统复习提纲与面试问答

> 使用方式：你先在主问题和追问的“回答”里写答案，我检查后会直接替换成更精确的版本。当前阶段以**学习理解**为主，不追求面试精简；回答会尽量包含**原理解释、具体例子、最后再总结关键词**。需要强调的关键词直接在原文中加粗。

## 一、知识树

### 1. 进程、线程、协程

- 进程是资源分配的基本单位
- 线程是 CPU 调度的基本单位
- 协程是用户态调度的轻量执行单元
- Java 平台线程和虚拟线程

### 2. 用户态、内核态、系统调用

- 用户态和内核态的权限差异
- 系统调用的作用
- 用户态和内核态切换成本
- 阻塞、唤醒和调度

### 3. 上下文切换

- 线程上下文切换
- 进程上下文切换
- 中断上下文
- 系统调用和进程切换的区别

### 4. 进程通信

- 管道
- 消息队列
- 共享内存
- 信号
- 信号量
- Socket

### 5. 内存与 I/O

- 虚拟内存
- 分页与页表
- 缺页异常
- Page Cache
- 阻塞 I/O、非阻塞 I/O、I/O 多路复用、异步 I/O
- select、poll、epoll

## 二、高频面试题

### 1. 进程和线程有什么区别？

回答：

进程和线程可以先从两个关键词理解：**进程是资源分配的基本单位，线程是 CPU 调度的基本单位**。

**进程**可以理解成一个正在运行的程序实例。操作系统会给每个进程分配相对独立的资源，比如虚拟地址空间、文件描述符、堆、代码段、数据段等。不同进程之间默认是隔离的，一个进程不能随便访问另一个进程的内存。

**线程**是进程内部的执行单元。一个进程里可以有多个线程，这些线程共享同一个进程的大部分资源，比如堆、方法区、打开的文件、网络连接等；但每个线程也有自己独立的执行现场，比如线程栈、程序计数器、寄存器上下文。

举个例子：浏览器是一个程序，它可以有多个进程，比如页面进程、插件进程、GPU 进程。某个页面进程里又可能有多个线程，比如渲染线程、JS 执行线程、网络线程。进程之间隔离更强，线程之间共享更多资源。

两者的主要区别：

1. **资源归属不同**：进程拥有独立地址空间和资源；线程共享所属进程的资源。
2. **调度粒度不同**：线程是 CPU 调度的基本单位；进程更多体现为资源容器。
3. **通信方式不同**：进程间通信通常需要 IPC，比如管道、消息队列、共享内存、信号、Socket；同一进程内线程可以直接读写共享变量，但必须处理同步问题。
4. **创建和切换成本不同**：进程创建和切换通常更重，因为涉及更多资源和地址空间；线程更轻，因为共享进程资源，只需要维护线程自己的栈和执行上下文。
5. **隔离性不同**：进程隔离性更好，一个进程崩溃通常不会直接破坏另一个进程；线程共享同一进程资源，一个线程写坏共享内存可能影响同进程的其他线程。

#### 追问 1：为什么线程切换通常比进程切换轻？

回答：

线程切换通常比进程切换轻，核心原因是：**同一进程内的线程共享地址空间，切换线程时通常不需要切换页表；进程切换通常要切换地址空间**。

进程切换时，操作系统要保存当前进程的上下文，再恢复另一个进程的上下文。除了寄存器、程序计数器、栈指针等执行现场，还可能需要切换页表。页表一换，CPU 之前缓存的虚拟地址到物理地址映射，也就是 **TLB**，可能大量失效。TLB 失效后，后续内存访问要重新查页表，成本更高。

线程切换时，如果两个线程属于同一个进程，它们共享同一套地址空间和页表，所以通常不需要切换页表，TLB 的影响更小。线程切换主要保存和恢复线程自己的寄存器、程序计数器、栈指针等。

但要注意，不能绝对说“线程切换时缓存和 TLB 一定不会失效”。线程切换仍然可能影响 CPU cache，因为不同线程访问的数据不同，会造成缓存命中率下降；只是相比进程切换，它通常少了地址空间切换这类重成本。

可以类比成：进程切换像换到另一家公司办公，要换工位、门禁、资料柜；线程切换像同一家公司不同员工轮流用同一间办公室，仍要换人和桌面资料，但不用换整套办公环境。

#### 追问 2：Java 中线程共享哪些数据，不共享哪些数据？

回答：

在 Java 中，同一个 JVM 进程里的多个线程共享**堆**和**方法区/元空间中的类元数据**，每个线程独享自己的**程序计数器、本地方法栈、Java 虚拟机栈**。

共享的部分：

1. **堆**：对象实例基本都在堆上，多个线程可以同时访问同一个对象。所以成员变量、集合对象、缓存对象如果被多个线程共享，就需要考虑线程安全。
2. **方法区/元空间相关数据**：类信息、运行时常量池、静态变量等是线程共享的。
3. **进程级资源**：比如打开的文件、Socket 连接等也可能被多个线程共享使用。

线程私有的部分：

1. **程序计数器**：记录当前线程执行到哪条字节码指令。
2. **Java 虚拟机栈**：每个方法调用会创建栈帧，局部变量表、操作数栈等在线程自己的栈里。
3. **本地方法栈**：服务 native 方法调用。

例子：方法里的局部变量 `int x = 1` 通常在线程自己的栈帧里，不会被其他线程直接访问；但如果局部变量引用了一个堆上的对象，比如 `User user = sharedUser`，那么 `user` 这个引用变量本身在线程栈里，但它指向的对象在堆上，其他线程也可能访问到这个对象。

#### 追问 3：一个线程崩溃会不会影响整个进程？

回答：

要分情况看。普通情况下，一个 Java 线程因为未捕获异常而结束，通常只会终止这个线程本身，不会直接让整个 JVM 进程退出。但如果这个线程破坏了共享状态，或者抛出的是严重错误，就可能间接影响整个进程。

比如一个普通工作线程执行任务时抛出 `RuntimeException`，没有捕获，这个线程会结束。线程池里的工作线程还可能被线程池补充一个新线程，整个进程不一定退出。

但下面几种情况可能影响整个进程：

1. **主线程退出且没有其他非守护线程**：JVM 可能结束。
2. **线程破坏共享数据**：比如一个线程更新共享 Map 时写了一半异常退出，其他线程读到不一致状态，业务可能整体出错。
3. **严重错误**：比如 `OutOfMemoryError`、`StackOverflowError`、JVM 崩溃、native 代码段错误，可能影响整个 JVM 进程。
4. **关键线程死亡**：比如负责接收请求、心跳、调度任务的核心线程异常退出且没有恢复机制，服务可能变得不可用。

所以更准确的说法是：**线程异常通常不会像进程隔离那样被完全隔离；一个线程普通异常可能只影响自己，但由于线程共享进程资源，严重错误或共享状态破坏可能影响整个进程**。

### 2. 用户态和内核态是什么？

回答：

用户态和内核态是操作系统为了保护内核和硬件资源，把程序运行权限分成的两种状态。可以先记住一句话：**用户态权限低，内核态权限高；普通应用运行在用户态，操作系统内核运行在内核态**。

在 x86 架构里，经常用 Ring 来表示权限级别。**用户态通常是 Ring 3**，权限最低；**内核态通常是 Ring 0**，权限最高。用户态程序不能直接执行特权指令，也不能随便访问内核空间和硬件设备。比如普通 Java 程序不能直接操作磁盘控制器、网卡、中断控制器，这些都要通过操作系统内核。

内核态可以访问内核空间，执行特权指令，管理 CPU、内存、磁盘、网络、进程调度等核心资源。比如读文件、创建线程、申请网络连接、等待 I/O，这些最终都需要内核参与。

举个例子：Java 代码调用 `socket.read()` 时，应用代码本身在用户态执行。但真正从网卡缓冲区取数据、判断是否阻塞、把数据从内核缓冲区拷贝到用户缓冲区，这些工作需要内核完成。所以程序会通过系统调用进入内核态，内核处理完再返回用户态。

可以类比成银行办业务：用户态像普通客户大厅，你不能自己进金库改账本；内核态像银行内部工作人员区域，可以操作核心账本和保险柜。你要取钱，需要通过柜台流程申请，而不是自己闯进金库。

#### 追问 1：为什么操作系统要区分用户态和内核态？

回答：

操作系统区分用户态和内核态，核心目的是**安全隔离和资源保护**。

如果所有程序都能直接访问硬件和内核内存，会有几个严重问题：

1. **安全问题**：恶意程序可以直接修改内核数据、读取其他进程内存、控制磁盘或网卡。
2. **稳定性问题**：普通程序一个 bug 就可能写坏操作系统核心数据，导致整台机器崩溃。
3. **资源管理问题**：多个程序都直接操作 CPU、内存、磁盘、网络，会互相抢资源，系统无法公平调度。
4. **隔离问题**：一个进程可能随意访问另一个进程的数据，用户隐私和进程边界都没有保障。

所以操作系统把普通应用限制在用户态。应用想读文件、发网络包、创建线程、申请内存，都必须通过系统调用向内核申请。内核会检查权限、参数和资源状态，再代表应用执行真正的底层操作。

比如你用 Java 打开一个文件，应用不能自己去磁盘上找扇区读取数据，而是调用 `read()`。内核会检查这个文件描述符是否合法、当前进程有没有权限、数据是否在 Page Cache 中，然后再返回结果。

#### 追问 2：系统调用和普通函数调用有什么区别？

回答：

普通函数调用和系统调用最大的区别是：**普通函数调用仍然在用户态内部执行；系统调用会从用户态切换到内核态，让操作系统内核帮你完成受保护的操作**。

普通函数调用，比如 Java 里调用一个普通方法，或者 C 里调用一个工具函数，本质上是在当前进程的用户空间里跳转执行代码。它不改变 CPU 权限级别，也不进入内核。

系统调用不一样。比如 `read()`、`write()`、`open()`、`socket()`、`fork()` 这些操作，普通程序没有权限直接完成。应用通常先调用一层库函数，比如 C 标准库里的 `read()` 包装函数，然后这个包装函数会通过特定 CPU 指令，比如 `syscall`、`sysenter` 或中断陷入机制，触发 CPU 从用户态切到内核态。内核根据系统调用号和参数执行对应逻辑，完成后再切回用户态。

举个例子：你调用普通函数 `add(a, b)`，它只是当前程序自己算一下 `a + b`。你调用 `read(fd, buf, size)`，它可能要检查文件描述符、查 Page Cache、等待磁盘 I/O、把内核缓冲区数据拷贝到用户缓冲区，这些都需要内核权限。

所以系统调用不是“某个普通函数比较特殊”这么简单，而是它背后会触发一次**用户态到内核态的权限切换**。

#### 追问 3：进入内核态一定很危险或一定要优化吗？

回答：

进入内核态不是危险行为，也不是一概都要优化。它是操作系统提供服务的正常机制。没有内核态，应用程序就没法安全地读写文件、收发网络包、创建线程、等待 I/O、进行进程调度。

真正需要关注的是：**高频、额外、可避免的用户态/内核态切换，以及由阻塞和唤醒引发的上下文切换**。

比如一次正常的文件读取需要系统调用，这是合理成本。但如果程序每次只读 1 个字节，循环调用 `read()` 一百万次，就会产生大量系统调用和状态切换，这种就应该优化，可以通过缓冲区批量读取减少次数。

再比如锁竞争。轻微竞争时，线程自旋一小会儿可能就拿到锁；如果立刻阻塞，线程会进入内核态挂起，之后还要被唤醒和调度，成本更高。所以很多锁实现会在短时间竞争时先尝试自旋，避免不必要的阻塞/唤醒。

可以总结为：

- **正常进入内核态**：读写文件、网络 I/O、线程调度、系统调用等，属于必要机制。
- **需要优化的情况**：过于频繁的小 I/O、无意义的系统调用、严重锁竞争、频繁阻塞唤醒、频繁上下文切换。

所以重点不是“不要进入内核态”，而是减少那些**高频且可以合并、缓存、异步化或避免的内核态切换**。

### 3. 什么是上下文切换？

回答：
进程切换时，

#### 追问 1：上下文里通常保存哪些内容？

回答：

#### 追问 2：上下文切换为什么会影响性能？

回答：

#### 追问 3：系统调用一定会导致进程切换吗？

回答：

### 4. 系统调用和进程切换有什么区别？

回答：

#### 追问 1：read 系统调用大致会经历什么过程？

回答：

#### 追问 2：阻塞 I/O 为什么可能导致线程被挂起？

回答：

#### 追问 3：为什么进程切换可能导致 TLB 或缓存失效？

回答：

### 5. 协程和线程有什么区别？

回答：

#### 追问 1：为什么协程切换成本更低？

回答：

#### 追问 2：协程适合 CPU 密集型任务吗？

回答：

#### 追问 3：Java 虚拟线程和传统平台线程有什么关系？

回答：

### 6. 进程间通信有哪些方式？

回答：

#### 追问 1：共享内存为什么快？

回答：

#### 追问 2：信号和信号量有什么区别？

回答：

#### 追问 3：Socket 为什么既能本机通信也能跨机器通信？

回答：

### 7. 虚拟内存解决了什么问题？

回答：

#### 追问 1：虚拟地址和物理地址有什么区别？

回答：

#### 追问 2：缺页异常是什么？

回答：

#### 追问 3：TLB 的作用是什么？

回答：

### 8. Page Cache 是什么？

回答：

#### 追问 1：Page Cache 为什么能提升文件 I/O 性能？

回答：

#### 追问 2：写文件成功是否一定代表数据已经落盘？

回答：

#### 追问 3：mmap 和 Page Cache 有什么关系？

回答：

### 9. 阻塞 I/O、非阻塞 I/O、I/O 多路复用有什么区别？

回答：

#### 追问 1：非阻塞 I/O 是否等于异步 I/O？

回答：

#### 追问 2：I/O 多路复用为什么适合高并发连接？

回答：

#### 追问 3：Java NIO 和 I/O 多路复用有什么关系？

回答：

### 10. select、poll、epoll 有什么区别？

回答：

#### 追问 1：select 的主要限制是什么？

回答：

#### 追问 2：epoll 为什么减少了无效遍历？

回答：

#### 追问 3：epoll 一定比 select 快吗？

回答：

## 三、待自查易错方向

> 这里只列检查方向，不写答案。你写完后，我会按这些点帮你抓错，并直接把回答替换成更精确的学习版。

- 进程和线程的资源边界
- 用户态、内核态、系统调用、进程切换的区别
- 上下文切换成本来自哪里
- 协程、虚拟线程、平台线程的关系
- 共享内存快但需要同步
- 非阻塞 I/O 和异步 I/O 的区别
- select、poll、epoll 的数据结构和触发方式

## 四、10 题自测

1. 进程和线程的本质区别是什么？
2. 为什么线程切换通常比进程切换轻？
3. 用户态和内核态为什么要隔离？
4. 系统调用一定会导致上下文切换吗？
5. 协程为什么比线程轻量？
6. 常见 IPC 方式有哪些，各自适合什么场景？
7. 虚拟内存解决了什么问题？
8. Page Cache 为什么能提升文件 I/O 性能？
9. 阻塞、非阻塞、同步、异步分别是什么意思？
10. select、poll、epoll 的核心差异是什么？
